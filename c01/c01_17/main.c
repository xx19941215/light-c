#include <stdio.h>

void main() {
    //标识符
    //对各种变量 函数的命名等命名时使用的字符序列称为标示符
    //凡是自己可以起名字的地方都叫标示符
}

// 说明
// 规则(必须遵守)
// 1.由26个英语字母大小写 0-9 _或者$组成
// 2.数字不可以开头
// 3.不可以使用关键字和保留字 但可以包含
// 4.严格区分大小写 长度无限制
// 5.不能包含空格
// 规范(必须遵守)
// 1.程序中不得出现仅靠大小写区分的标示符
// 2.所有宏定义 枚举常数 常量 全部使用大写字母命名 用下划线分割单词
// 3.定义变量别忘了初始化 定义变量时编译器并不一定清空了这段内存 他可能是无效的数据 运行程序 会无效退出
// 4.变量名、函数名、多单词组成时 一般使用小驼峰命名发。